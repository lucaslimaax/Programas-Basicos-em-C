#include <stdio.h>
 main()
  {float b, h, area;
  printf("entre com o valor da base:");
  scanf("%f",&b);
  printf("entre com o valor da altura:");
  scanf("%f",&h);
  area=(b*h)/2;
  printf("a area=%.2f",area);
  system("pause >> log");
  }
